{
  "name": "@unconfirmed/kei",
  "version": "0.0.9",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "prepublishOnly": "bun run build",
    "build": "bun build src/index.ts --outdir dist --target node --external '@noble/*' --external '@mysten/*' && bunx tsc --emitDeclarationOnly --outDir dist",
    "test": "bun test"
  },
  "dependencies": {
    "@mysten/bcs": "^1.3.0",
    "@noble/curves": "^1.9.0",
    "@noble/hashes": "^1.7.0"
  },
  "devDependencies": {
    "@mysten/sui": "^2.13.0",
    "bun-types": "^1.3.11"
  }
}
